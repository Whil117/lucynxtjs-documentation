# Components

Exist a lot of components for you. You can combine with other atoms without problems.
All Components has the same props if you used html only. But with new properties such:

CustomCSS, customCSSLabel, customCSSOption, customCSSError, css.

These properties recives a function and should return a css

You can use function css from Emotion

```js
import { css } from "@emotion/react";
```

## AtomText

It's a great component because you have the typical properties for example: width, textAlign, textDecoration and more as params. This Component contains customCSS if you want to more customization

```javascript
<AtomText
  customCSS={(css) => css`
    background-color: red;
  `}
>
  Hello Lucy
</AtomText>
```

export const ExampleTextAtom = () => {
return (

<AtomText
customCSS={(css) => css`    background-color: red;
 `}

> Hello Lucy

</AtomText>
) };

<ExampleTextAtom />

## AtomButton

My favorite component, because has some improviments. When you have a background color. and this color is awful with the text button, AtomButton has a function that takes backgroundColor, backgroundLinearGradient and convert if the color is black or white

```js
<AtomButton
  isFocus
  backgroundLinearGradient={{
    rotate: "315deg",
    secondary: "#07deff",
    primary: "#0f97ff",
  }}
  padding="10px 15px"
>
  Button example
</AtomButton>
```

import { AtomButton,AtomWrapper, AtomText, AtomIcon, AtomImage,AtomLoader } from "lucy-nxtjs";
import isDarkLight from "lucy-nxtjs/build/src/@utils/isDarkLight";

export const Counter = () => {

return (

<AtomWrapper height="auto" flexDirection="row" gap="10px" justifyContent="flex-start" flexWrap="wrap">

<AtomButton

isFocus
backgroundLinearGradient={{
    rotate: "315deg",
    secondary: "#07deff",
    primary: "#0f97ff",
  }}
padding="10px 15px"

> Button example

</AtomButton>
<AtomButton

isFocus
backgroundLinearGradient={{
    rotate: "315deg",
       secondary: "#76f66a",
          primary: "#76f6",
  }}
padding="10px 15px"

> Button example

</AtomButton>
<AtomButton

isFocus
backgroundColor="#e92929"
padding="10px 15px"

> Button example

</AtomButton>
<AtomButton

isFocus
backgroundColor="#07deff"
padding="10px 15px"

> Button example

</AtomButton>
<AtomButton

isFocus
backgroundColor="#eadc0c"
padding="10px 15px"

> Button example

</AtomButton>
<AtomButton

isFocus
backgroundColor="#16ec2b"
padding="10px 15px"

> Button example

</AtomButton>

</AtomWrapper>
) };

<Counter />

## AtomWrapper

The best component, You always going to work with this atom, this atom is a special of container or div. With CustomCSS you have a powerfull container
Compatible with flex and grid and draggable container provide by Framer Motion

```js
<AtomWrapper gap="5px" draggable>
  <AtomWrapper draggable backgroundColor="#07deff" padding="5px">
    <AtomText>Default Text</AtomText>
  </AtomWrapper>
  <AtomWrapper
    draggable
    customCSS={(css) => css`
      border: 1px solid #07deff;
    `}
    padding="5px"
  >
    <AtomText color="white">Default Text</AtomText>
  </AtomWrapper>
</AtomWrapper>
```

export const ExampleWrapper = () => {
return (
<AtomWrapper gap="5px" draggable>

<AtomWrapper draggable backgroundColor="#07deff" padding="5px">
  <AtomText>Default Text</AtomText>
</AtomWrapper>
<AtomWrapper draggable customCSS={(css)=> css`
border:1px solid #07deff;
`} padding="5px">
  <AtomText color="white">Default Text</AtomText>
</AtomWrapper>
</AtomWrapper>
) };

<ExampleWrapper />

## AtomIcon

If you want to render a svg, you can use with a external url, This is great because you can chache the asset and improve the page

You have more options as: color fill by default black, width, height. with a customCSS

This Atom use swr library for fetching the icon and embed the svg inside a dangerouslySetInnerHTML with a AtomWrapper

```js
<AtomIcon
  src="https://res.cloudinary.com/whil/image/upload/v1679173748/GITHUB_udwkxu.svg"
  color="#0072FF"
/>
```

export const ExampleIcons = () => {
return (

<AtomWrapper flexDirection="row" width="auto" justifyContent="flex-start" gap="10px">
  <AtomIcon color="default" />
  <AtomIcon
    src="https://res.cloudinary.com/whil/image/upload/v1679173748/GITHUB_udwkxu.svg"
    color="#0072FF"
  />

{" "}

<AtomIcon
  src="https://res.cloudinary.com/whil/image/upload/v1676009859/MOVE_y3ho0n.svg"
  color="#0072FF"
/>
<AtomIcon
  src="https://res.cloudinary.com/whil/image/upload/v1661923746/spotify_jomqes.svg"
  color="default"
/>
<AtomIcon
  src="https://res.cloudinary.com/whil/image/upload/v1676008336/CURSOR_m7mjkg.svg"
  color="white"
/>
<AtomIcon
  src="https://res.cloudinary.com/whil/image/upload/v1676009019/EDITYOR_e4pyzh.svg"
  color="white"
/>
<AtomIcon
  src="https://res.cloudinary.com/whil/image/upload/v1676009570/UPDATEE_mlumua.svg"
  color="white"
/>

</AtomWrapper>

)
}

<ExampleIcons />

## AtomImage

Typically a web page use images, using a external URL image. you can render a imagen with a lot of properties as: ObjectFit, width, Height, customCSS and more. This component has cache and improve the page

```js
<AtomImage src="https://picsum.photos/200/300" width="auto" />
```

export const ExampleImages = () => {
return (

<AtomWrapper
  flexDirection="row"
  width="auto"
  justifyContent="flex-start"
  gap="10px"
>
  <AtomImage
    src="https://picsum.photos/200/300"
    width="auto"
/>

</AtomWrapper>

)
}

<ExampleImages />

## AtomLoader

It's neccessary use a loader when you are doing operations with your API. So This component works for you.

```js
  <AtomLoader type="small" isLoading colorLoad="white" />
  <AtomLoader type="small" isLoading colorLoad="#0072FF" />
  <AtomLoader type="small" isLoading colorLoad="#ef2323" />
  <AtomLoader type="small" isLoading colorLoad="#82ef23" />
  <AtomLoader type="small" isLoading colorLoad="#23d7ef" />
  <AtomLoader type="small" isLoading colorLoad="#ef23d4" />
  <AtomLoader type="small" isLoading colorLoad="#cadd23" />
```

export const ExampleLoader = () => {
return (

<AtomWrapper
  flexDirection="column"
  width="auto"
  justifyContent="flex-start"
  gap="10px"
>
<AtomText color="white">
Type Small
</AtomText>
<AtomWrapper
  flexDirection="row"
  width="auto"
  justifyContent="flex-start"
  gap="10px"
>

  <AtomLoader type="small" isLoading colorLoad="white" />
  <AtomLoader type="small" isLoading colorLoad="#0072FF" />
  <AtomLoader type="small" isLoading colorLoad="#ef2323" />
  <AtomLoader type="small" isLoading colorLoad="#82ef23" />
  <AtomLoader type="small" isLoading colorLoad="#23d7ef" />
  <AtomLoader type="small" isLoading colorLoad="#ef23d4" />
  <AtomLoader type="small" isLoading colorLoad="#cadd23" />
</AtomWrapper>

<AtomText color="white">Type medium</AtomText>
<AtomWrapper
  flexDirection="row"
  width="auto"
  justifyContent="flex-start"
  gap="10px"
>
  <AtomLoader type="medium" isLoading colorLoad="white" />
  <AtomLoader type="medium" isLoading colorLoad="#0072FF" />
  <AtomLoader type="medium" isLoading colorLoad="#ef2323" />
  <AtomLoader type="medium" isLoading colorLoad="#82ef23" />
  <AtomLoader type="medium" isLoading colorLoad="#23d7ef" />
  <AtomLoader type="medium" isLoading colorLoad="#ef23d4" />
  <AtomLoader type="medium" isLoading colorLoad="#cadd23" />
</AtomWrapper>

<AtomText color="white">Type large</AtomText>
<AtomWrapper
  flexDirection="row"
  width="auto"
  justifyContent="flex-start"
  gap="10px"
>

  <AtomLoader type="large" isLoading colorLoad="white" />
  <AtomLoader type="large" isLoading colorLoad="#0072FF" />
  <AtomLoader type="large" isLoading colorLoad="#ef2323" />
  <AtomLoader type="large" isLoading colorLoad="#82ef23" />
  <AtomLoader type="large" isLoading colorLoad="#23d7ef" />
  <AtomLoader type="large" isLoading colorLoad="#ef23d4" />
  <AtomLoader type="large" isLoading colorLoad="#cadd23" />
</AtomWrapper>

</AtomWrapper>

)
}

<ExampleLoader />

import {useState} from "react"

export const ExampleWithOthers = () => {

const [state,setState] = useState({
isShow:false,
color:"#0072FF"
})

return (

<AtomWrapper
  flexDirection="column"
  width="auto"
  justifyContent="flex-start"
  gap="10px"
>
 <AtomLoader type="fullscreen"   isLoading={state.isShow}     onClose={() => {
          setState({
            isShow: false,
            color: "#0072FF",
          });
        }} colorLoad={state.color}/>
<AtomText color="white">
Type fullscreen
</AtomText>
<AtomWrapper
  flexDirection="row"
  width="auto"
  justifyContent="flex-start"
  gap="10px"
>

  <AtomButton backgroundColor="#ffffff" onClick={()=>{
    setState({
    isShow:true,
    color:"#ffffff"
    })
  }}>
  <AtomLoader type="small" isLoading colorLoad={isDarkLight("#ffffff")}/>
  </AtomButton>
  <AtomButton backgroundColor="#ef2323" onClick={()=>{
    setState({
    isShow:true,
    color:"#ef2323"
    })
  }}>
  <AtomLoader type="small" isLoading colorLoad={isDarkLight("#ef2323")} />
  </AtomButton>
    <AtomButton backgroundColor="#0072FF" onClick={()=>{
    setState({
    isShow:true,
    color:"#0072FF"
    })
  }}>
  <AtomLoader type="small" isLoading colorLoad={isDarkLight("#0072FF")} />
  </AtomButton>
     <AtomButton backgroundColor="#82ef23" onClick={()=>{
    setState({
    isShow:true,
    color:"#82ef23"
    })
  }}>
  <AtomLoader type="small" isLoading colorLoad={isDarkLight("#82ef23")} />
  </AtomButton>
      <AtomButton backgroundColor="#23d7ef" onClick={()=>{
    setState({
    isShow:true,
    color:"#23d7ef"
    })
  }}>
  <AtomLoader type="small" isLoading colorLoad={isDarkLight("#23d7ef")} />
  </AtomButton>
        <AtomButton backgroundColor="#ef23d4" onClick={()=>{
    setState({
    isShow:true,
    color:"#ef23d4"
    })
  }}>
  <AtomLoader type="small" isLoading colorLoad={isDarkLight("#ef23d4")} />
  </AtomButton>
        <AtomButton backgroundColor="#cadd23" onClick={()=>{
    setState({
    isShow:true,
    color:"#cadd23"
    })
  }}>
  <AtomLoader type="small" isLoading colorLoad={isDarkLight("#cadd23")} />
  </AtomButton>
</AtomWrapper>

</AtomWrapper>

)
}

<ExampleWithOthers />

## AtomModal

Sometimes you can show a special data or alert. so you can use AtomModal to show using only a state, and you can write a message or anything

```js
<AtomWrapper>
  <AtomModal
    isShow={show}
    onCloseShow={() => {}}
    customCSS={(css) => css`
      background-color: #fff;
      height: 217px;
      width: 420px;
      padding: 30px 20px;
      gap: 10px;
    `}
  >
    <AtomWrapper
      height="auto"
      customCSS={(css) => css`
        flex: 1;
        width: -webkit-fill-available;
        justify-content: flex-start;
        gap: 10px;
      `}
    >
      <AtomText fontSize="16px" fontWeight="bold">
        Error!
      </AtomText>
      <AtomWrapper
        customCSS={(css) => css`
          background-color: black;
          height: 1px;
        `}
      >
        {" "}
      </AtomWrapper> <AtomText fontSize="12px">
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Corrupti
        deserunt, ipsum eligendi debitis porro nobis suscipit tenetur
        repellendus, rem reiciendis, quae distinctio quo quod quasi sapiente
        error recusandae alias fuga!
      </AtomText>
      <AtomWrapper
        flexDirection="row"
        height="auto"
        justifyContent="flex-end"
        gap="20px"
      >
        <AtomButton
          onClick={() => {
            setShow(false);
          }}
        >
          Cancel
        </AtomButton>

        <AtomButton
          onClick={() => {
            setShow(false);
          }}
          isFocus
          backgroundColor="#ef2323"
        >
          Ok
        </AtomButton>
      </AtomWrapper>
    </AtomWrapper>
  </AtomModal>
  <AtomButton
    onClick={() => {
      setShow(true);
    }}
  >
    Click Me
  </AtomButton>
</AtomWrapper>
```

import { AtomModal } from "lucy-nxtjs";

export const ExampleModal = () => {
  const [show, setShow] = useState(false);
  return (
<AtomWrapper>
  <AtomModal
    isShow={show}
    onCloseShow={() => {}}
    customCSS={(css) => css`
      background-color: #fff;
      height: 217px;
      width: 420px;
      padding: 30px 20px;
      gap: 10px;
    `}
  >
    <AtomWrapper
      height="auto"
      customCSS={(css) => css`
        flex: 1;
        width: -webkit-fill-available;
        justify-content: flex-start;
        gap: 10px;
      `}
    >
      <AtomText fontSize="16px" fontWeight="bold">
        Error!
      </AtomText>
      <AtomWrapper
        customCSS={(css) => css`
          background-color: black;
          height: 1px;
        `}
      >
        {" "}
      </AtomWrapper> <AtomText fontSize="12px">
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Corrupti
        deserunt, ipsum eligendi debitis porro nobis suscipit tenetur
        repellendus, rem reiciendis, quae distinctio quo quod quasi sapiente
        error recusandae alias fuga!
      </AtomText>
      <AtomWrapper
        flexDirection="row"
        height="auto"
        justifyContent="flex-end"
        gap="20px"
      >
        <AtomButton
          onClick={() => {
            setShow(false);
          }}
        >
          Cancel
        </AtomButton>

        <AtomButton
          onClick={() => {
            setShow(false);
          }}
          isFocus
          backgroundColor="#ef2323"
        >
          Ok
        </AtomButton>
      </AtomWrapper>
    </AtomWrapper>

  </AtomModal>
  <AtomButton
    onClick={() => {
      setShow(true);
    }}
  >
    Click Me
  </AtomButton>
</AtomWrapper>
);
};

<ExampleModal />

## AtomInput

Oh yeah, A web application needs a inputs, You have a lot of variants. for example:

```md
"button"
"checkbox"
"color"
"date"
"datetime-local"
"email"
"file"
"hidden"
"image"
"month"
"number"
"password"
"radio"
"range"
"reset"
"search"
"submit"
"tel"
"text"
"time"
"url"
"week"
"select"
"textarea";
```

Exist a lot of input types, And includes special parameters for example:

### CustomCSS

You can edit css style of the Wrapper input, for example background, width, height.

import { AtomInput } from "lucy-nxtjs";
import { css } from "@emotion/react";

export const ExampleInputText = () => {
  return (
    <AtomInput
      type="text"
      label="Name"
      labelColor="black"
      labelBackground="white"
      labelFontFamily="Inter"
      labelFontSize="12px"
      labelFontWeight="bold"
      customCSSError={(css) => css`
        /* You custom CSS Error*/
      `}
      customCSSLabel={(css) => css`
        /* You custom CSS*/
      
         font-family: ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Noto Sans,Ubuntu,Cantarell,Helvetica Neue,Arial,sans-serif,Apple Color Emoji,Segoe UI Emoji,Segoe UI Symbol,Noto Color Emoji;

      `}
      customCSS={(css) => css`

      `}
      css={() => css`
       border:1px solid black;
         color:white;
      `}
      isFocus
    />

);
};

<ExampleInputText />

### CustomCSSLabel

Edit the style of the label input.

export const ExampleInputTextLabel = () => {
  return (
    <AtomInput
      type="text"
      label="StyleLabelInput"
      labelColor="black"
      labelBackground="white"
      labelFontFamily="Inter"
      labelFontSize="12px"
      labelFontWeight="bold"
      customCSSError={(css) => css`
        /* You custom CSS Error*/
      `}
      customCSSLabel={(css) => css`
        /* You custom CSS*/
        background-color:red;
         font-family: ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Noto Sans,Ubuntu,Cantarell,Helvetica Neue,Arial,sans-serif,Apple Color Emoji,Segoe UI Emoji,Segoe UI Symbol,Noto Color Emoji;

      `}
      customCSS={(css) => css`

      `}
      css={() => css`
       border:1px solid black;
         color:white;
      `}
      isFocus
    />

);
};

<ExampleInputTextLabel />

### CustomCSSError

When you are validating the input field

export const ExampleInputTextError = () => {
  return (
    <AtomInput
      type="text"
      label="Name"
      labelColor="black"
      labelBackground="white"
      labelFontFamily="Inter"
      labelFontSize="12px"
      labelFontWeight="bold"
      onError={()=>"Name is a require field"}
      customCSSError={(css) => css`
        /* You custom CSS Error*/
      `}
      customCSSLabel={(css) => css`
        /* You custom CSS*/
         font-family: ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Noto Sans,Ubuntu,Cantarell,Helvetica Neue,Arial,sans-serif,Apple Color Emoji,Segoe UI Emoji,Segoe UI Symbol,Noto Color Emoji;

      `}
      customCSS={(css) => css`

      `}
      css={() => css`
       border:1px solid black;
         color:white;
      `}
      isFocus
    />

);
};

<ExampleInputTextError />

### CustomCSSOption

This works when you select a input select, If you dont selected a value, by default value is "DEFAULT", with the function options recive a array with the follow params such: id, label, value

export const ExampleInputTextSelect = () => {
  return (
    <AtomInput
      type="select"
      label="Name"
      labelColor="black"
      labelBackground="white"
      labelFontFamily="Inter"
      labelFontSize="12px"
      labelFontWeight="bold"
      onError={()=>"Name is a require field"}
      customCSSError={(css) => css`
        /* You custom CSS Error*/
      `}
      customCSSLabel={(css) => css`
        /* You custom CSS*/
         font-family: ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Noto Sans,Ubuntu,Cantarell,Helvetica Neue,Arial,sans-serif,Apple Color Emoji,Segoe UI Emoji,Segoe UI Symbol,Noto Color Emoji;

      `}
      customCSS={(css) => css`

      `}
      value="DEFAULT"
      options={()=>[

        {
          "id":2,
          label:"Option 1",
          value:"Optio"
        }
      ]}
      css={() => css`
       border:1px solid black;
         color:white;
      `}
      isFocus
    />

);
};

<ExampleInputTextSelect />

### css

If don't like the style by default, you can edit this style with the function css

export const ExampleInputTextCore = () => {
  return (
    <AtomInput
      type="select"
      label="Name"
      labelColor="black"
      labelBackground="white"
      labelFontFamily="Inter"
      labelFontSize="12px"
      labelFontWeight="bold"
      customCSSError={(css) => css`
        /* You custom CSS Error*/
      `}
      customCSSLabel={(css) => css`
        /* You custom CSS*/
         font-family: ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Noto Sans,Ubuntu,Cantarell,Helvetica Neue,Arial,sans-serif,Apple Color Emoji,Segoe UI Emoji,Segoe UI Symbol,Noto Color Emoji;

      `}
      customCSS={(css) => css`

      `}
      value="DEFAULT"
      options={()=>[

        {
          "id":2,
          label:"Option 1",
          value:"Optio"
        }
      ]}
      css={() => css`
      color:white;
       background-color:#0072FF;
      `}
      isFocus
    />

);
};

<ExampleInputTextCore />

```js
<AtomInput
  type="text"
  label="Name"
  labelColor="black"
  labelBackground="white"
  labelFontFamily="Inter"
  labelFontSize="17px"
  labelFontWeight="bold"
  onError={() => "Name is require field"}
  customCSSError={() => css`
    /* You custom CSS Error*/
  `}
  customCSSLabel={() => css`
    /* You custom CSS*/
  `}
  customCSS={(css) => css`
    width: 100px;
  `}
  value={state}
  isFocus
  onChange={(event) => setstate(event.target.value)}
/>
```

## AtomLink

Exclusive for the framework Next.js, Sometimes you need apply styles to the link. but Next.js has a basically styles using styles inline. But if you want to add customCSS without styles inline you have the possibility.

You have two properties, Link and href.

### Link

If you want to navigate inside your application, you should use property link

### Href

If you have a external url, you should use a href property.

```js
<AtomWrapper>
  <AtomLink
    link="#atomlink"
    textDecoration="none"
    customCSS={() => css`
      color: #cadd23;
      width: 100%;
    `}
  >
    Click me
  </AtomLink>
  <AtomLink
    href="https://www.linkedin.com/in/ivangarciawhil117/"
    textDecoration="none"
    customCSS={() => css`
      color: #cadd23;
      background-color: #0072ff;
    `}
  >
    My Linkedin
  </AtomLink>
</AtomWrapper>
```

import { AtomLink } from "lucy-nxtjs";
export const ExampleInputLink = () => {
  return (
    <AtomWrapper>
      <AtomLink
        link="#atomlink"
        textDecoration="none"
        customCSS={() => css`
          color: #cadd23;
          width: 100%;
        `}
      >
        Click me
      </AtomLink>
      <AtomLink
        href="https://www.linkedin.com/in/ivangarciawhil117/"
        textDecoration="none"
        customCSS={() => css`
          color: #cadd23;
          background-color: #0072ff;
        `}
      >
        My Linkedin
      </AtomLink>
    </AtomWrapper>
  );
};

<ExampleInputLink />
